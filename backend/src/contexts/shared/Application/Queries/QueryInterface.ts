export interface QueryInterface {
  // This could or could not extend the EventInterface.
  // Example. we might want to force Queries to have "executedBy" parameter, so we would have a specific interface for that
  // Or we might want to have a generic extra fields for all Events.
}
